<!-- 任务⑥ 执行手册 · 规划于 2026-05-28 · 纯前端 · 待主线 ①–⑤ 收尾后由执行端按本手册实施 · 源方案副本: ~/.claude/plans/users-horacedong-downloads-3-on-questio-eager-turing.md -->

# 任务⑥ 执行手册 — 辅盘新增「卜卦盘」+「择日盘」（西洋卜卦/择日完整规则库）

> **给执行 Claude Code 的直接照做手册。** 按 milestone（M0→M6）顺序做；每个 `- [ ]` 是一个可独立完成、可验证的 task，做完打勾。每个 task 自带 **DONE WHEN** 自测；每个 milestone 末跑 **§V 验证套件**。建议用 TaskCreate 把 M0–M6 建成 7 条跟踪任务。
> 全程参照 6 份规格/源文件（见 §附录-2，在 `~/Downloads`）。**纯前端，零后端改动。**

---

## §0 开工须知（先读 1 分钟，全程生效）

1. **纯前端**：只改 `Horosa-Web/astrostudyui/`。**不碰** `astropy/`（Python）、`astrostudysrv/`（Java）、Tauri 壳、jar、嵌入式 runtime——后端 `/chart` 已产出尊贵/相位(含入出相)/接纳/互溶/映点/被夹/月空亡/游走/日时主/逆行；缺的燃烧·角续果·东西出全在前端从经度算。
2. **两页自包含、时间独立**：`HoraryMain`/`ElectionMain` 各自持本地 `fields`+`chart`，自调 `fetchChart`，**不回灌父级 astro model**（不污染主盘）。初值用 `props.fields` 播种。
3. **范围**：卜卦 = Sibly 71 + Dorotheus 43 + Sahl 全部 + Hephaistion 补充 + 盗窃失物 11 步；择日 = 择日清单 §1–§10 全套（含恒星/中点/本命结合/消去法）。**延后（v1 不做）**：择日 §11 高级（中国七政四餘/玄空/时势择日/推运）、卜卦 Omar 本命三卷（且这些 app 主限/法达/小限/返照模块可能已实现，勿重写）。
4. **铁律**：① 术数语义色不动（只动结构/中性间距）；② 择日 §6.16 就职**禁写政治人物名**，用中性示例；③ 算法**自写**，描述类中文译文**直抄**（planet_in_sign 等）且每条带 `source_ref`；④ 裁决**只给证据+倾向，不下命定结论**，势均力敌建议「另择时」；⑤ 量化（完成度%/评分0-100/件数奇偶）用显式数字；⑥ 收尾出 `docs/*-v<ver>.md` + `windows-sync-handoff` 条目（注「纯前端」）→ 过 `release_preflight.sh` → **push/tag/publish 前问用户**。
5. **改完即验**：每个 task 按其 DONE WHEN 自测；UI 类用 preview MCP（`horosa-dev` skill 起栈），明暗双色都看。

### §0.1 复用速查表（贴手边）
| 用途 | 复用对象 | 路径 |
|---|---|---|
| 取盘（含缓存） | `fetchChart(values, opts)` | `services/astro.js:43` |
| 请求体组装 | astro model 发 `/chart` 的 effect（**只读照搬** values 组装） | `models/astro.js` |
| 中栏圆盘 | `<AstroChart value={chart} chartDisplay chartStyle planetDisplay lotsDisplay showAstroMeaning height/>` | `components/astro/AstroChart.js`（用法见 `AstroChartMain.js:952`） |
| 时间调整 | `PlusMinusTime`（含 `adjustOnly` inline 微调，`hook`） | `components/astro/PlusMinusTime.js`（用法 `AstroChartMain.js:455,482`） |
| 地点 | `GeoCoordModal` | 用法 `AstroChartMain.js:489` |
| 黄道/宫制/样式/按钮/图标/页签 | `XQSelect/Option`、`XQSegmented`、`XQButton`、`XQIcon`、`XQTabs` | `components/xq-ui`、`components/xq-icons` |
| 3 栏栅格 | `.horosa-astro-redesign-grid`（三列）、`.horosa-chart-stage-redesign` | `layouts/app.less:4646` |
| 右栏 tab + 正负计数套路 | `<Tag color='green'>{n}项</Tag>`/`color='volcano'` | 仿 `dunjia/DunJiaMain.js:1964`、`lrzhan/LiuRengMain.js:4777` |
| chart 字段解析范例 | receptions/mutuals/antiscias/surround/dignity/isVOC 读法 | `utils/astroAiSnapshot.js:164-523` |
| AI 快照 | `saveModuleAISnapshot(name, text, meta)` | `utils/moduleAiSnapshot.js:48` |
| 占星词汇（行星/宫位中文名等，已有则复用） | `AstroMsgCN` | `constants/AstroText.js` |

### §0.2 目标文件树（最终长这样）
```
components/horary/HoraryMain.js          components/horary/HoraryJudgment.js
components/election/ElectionMain.js      components/election/ElectionJudgment.js
divination/data/      planets signs dignities aspects lots houseMeanings bodyParts shipParts
                      directions fixedStars midpoints topicMaster castMoments glossary
                      decanImages planetaryHours dorotheusDegrees   (.js)
divination/engine/    chartFacts conditions moon aspectsEngine reception perfection radicality utils (.js)
divination/horary/    horaryJudgments theftModule describe timing horaryEngine horarySnapshot (.js)
divination/election/  modules hardFlags scoring rulePacks natalIntegration workflow electionEngine electionSnapshot (.js)
```
改：`components/auxchart/AuxChartMain.js`、`utils/aiExport.js`、按需 `layouts/app.less`（加 `.horosa-horary-page`/`.horosa-election-page` 修饰类，只动间距）。

---

## M0 — 注册 + 双页空壳（先跑通骨架）

- [ ] **T0.1 在 `components/auxchart/AuxChartMain.js` 注册两页（4 处编辑）**
  - 顶部 import：`import HoraryMain from '../horary/HoraryMain';` `import ElectionMain from '../election/ElectionMain';`
  - 第 12 行 `AUX_TABS` 末尾加 `'horary','election'`。
  - 13–19 行 `AUX_QUICK_ACTIONS` 在 `otherbu`（骰子）后加：`{key:'horary',label:'卜卦盘',icon:'gua'}`、`{key:'election',label:'择日盘',icon:'clock'}`（先到 `components/xq-icons/` 确认图标名存在，否则换现成名如 `quickAi`/`astro`，**勿传不存在的 icon**）。
  - 31–47 行 `state.hook` 加 `horary:{fun:null}, election:{fun:null}`。
  - 199 行 otherbu 的 `</TabPane>` 后加两块 `<TabPane tab="卜卦盘" key="horary"><HoraryMain .../></TabPane>` 与 election 版；props 传 `fields/fieldsAry/height={childHeight}/chartDisplay/planetDisplay/lotsDisplay/showAstroMeaning/hook={this.state.hook.X}/dispatch`（**不传 chart**，两页自取）。
  - **DONE WHEN**：骰子下方出现「卜卦盘」「择日盘」两按钮，点击不报错。
- [ ] **T0.2 建 `components/horary/HoraryMain.js`（自包含 3 栏壳）**
  - state：`{ localFields, chart:null, judgment:null, rightTab:'1', questionCategory, loading }`；`localFields` 从 `props.fields` 深拷贝，默认 `tradition=true, zodiacal=0`。
  - `buildReqValues(localFields)`：照搬 `models/astro.js` 里发 `/chart` 的 values 组装（只读参考）。`componentDidMount` 与 localFields 变化后 → `fetchChart(buildReqValues(localFields), {cache:true})` → `setState({chart})`。
  - 布局：外层 `.horosa-horary-page`，内 `.horosa-astro-redesign-grid`：左栏（`PlusMinusTime` 调时 + `GeoCoordModal` 地点 + `XQSelect` 黄道/宫制 + `XQSegmented` 样式 + `XQButton` 重算）；中栏 `.horosa-chart-stage-redesign` 内 `<AstroChart value={this.state.chart} .../>`；右栏 `<XQTabs>` 占位。
  - **DONE WHEN**：卜卦盘画出圆盘，左栏改时间→盘重算刷新。
- [ ] **T0.3 建 `components/election/ElectionMain.js`**（复制 T0.2 改造）
  - 默认 `house_system=whole_sign`、`zodiacal=0`、传统守护。左栏额外占位：用事类型 `XQSelect`（先放静态几项）+ 时间窗输入（起止日期/时段，先占位）。
  - **DONE WHEN**：择日盘画出圆盘+独立时间控件。
- [ ] **验证 M0**：preview MCP 起栈→辅盘→两页，`preview_snapshot`/`preview_screenshot` 明暗双色；确认两页时间互不影响、不影响主占星盘。

---

## M1 — 数据底座 `divination/data/`（纯静态表，先做扎实）

> 抽取法：先用源文件**顶部中文摘要**填表，再用**双语正文**校对补全。字段名用英文。

- [ ] **T1.1 `planets.js`**：8 条（含交点）。`{id,cn,benefic,sect_pref,gender,qualities,color,natural_sig,keywords[],industries[],body_parts[]}` + 逆行口径（水逆避签约/沟通/购车…）。源：主清单 §1.1 + 择日 §2.1。
- [ ] **T1.2 `signs.js`**：12 条。`element/modality/gender/sect/crooked_straight/fertility/bicorporeal/shape/body_part/direction/ship_part` + 三方四正用法（固定=择日最佳、Taurus 第一；变动婚/事业忌）+ 命度度数规则（避 28°后、固定取 1–5°）。源：主清单 §1.2 + 择日 §2.2。
- [ ] **T1.3 `dignities.js`**：12 座×五尊贵（界=埃及；三分昼/夜/共；面）。前端**主要读后端 `obj.dignities/selfDignity`**，本表供 describe/topic 查「某座守护星」。源：主清单 §1.3。
- [ ] **T1.4 `aspects.js` + `lots.js`**：5 相位+映点+容许度（可配，默认日15/月12/水金7/火8/木土9，两星取平均）；福点=ASC+Moon−Sun（**夜盘反转**）等 + **支持查点的定位星 dispositor**。源：主清单 §1.4/§1.5 + 择日 §2.4。
- [ ] **T1.5 `houseMeanings.js`**：每宫 `natural_sig/co_significators/angularity` + **`turnedHouse(baseHouse,offset)` 转宫**（被问对象之事=以其本宫为第 1 宫顺数，如兄弟的财产=本盘 4 宫；§3 选征象星与痣验证被问之人全用它）+ 公司各宫/汽车各宫（择日事业·购车用）。源：主清单 §1.9 + 择日 §2.3。
- [ ] **T1.6 `bodyParts.js`/`shipParts.js`/`directions.js`**：身体部位 + **度数定位（早=上/中=中/晚=下）**；船只 12 部位；方位**三套**（按宫位/按元素/按月亮四轴 ASC-MC=东…）。源：主清单 §1.6/§1.7/§1.8。
- [ ] **T1.7 `fixedStars.js` + `midpoints.js`**：恒星 10 主+杨国正扩展（`{name,nature[],meaning,lon_1995,declination,election:{avoid,good_for[]}}`，附**岁差 50.27″/年**换算）；婚姻/创业吉中点 + 含义字典。源：择日 §2.6/§2.8。
- [ ] **T1.8 `topicMaster.js` + `castMoments.js` + `glossary.js`**：27 用事索引（`natural_significators/key_houses/preferred_asc_modality/moon_phase_pref/must_avoid/must_have/special_aspect`）；起盘时刻表（topic→「哪一刻=这张盘」）；术语 tooltip 字典。源：择日 §2.9/§8/§10.2。
- [ ] **T1.9 `decanImages.js` + `planetaryHours.js` + `dorotheusDegrees.js`**：Sahl A.1 面意象 36 条 + A.2 行星小时 7×3（已是完整数据直录）；Dorotheus **Ch18 堕胎危险度数表**（白羊1-11°/双子全度/巨蟹1-15°/狮子1-11°/处女全度/天秤·天蝎11-30°/射手1-11°/摩羯1-15°/水瓶20-30°）+ **Ch37-40 手术宜忌座**（卜卦与择日 surgery 共用）。
- [ ] **验证 M1**：写临时自测脚本——喂一个 `/chart` 结果，能查到任意行星尊贵、界主星、某座守护、`turnedHouse`、身体部位（含上中下）、恒星 1995→当年位置、任一 topic 包字段。

---

## M2 — substrate 引擎 `divination/engine/`（卜卦+择日共用；Jest 覆盖）

- [ ] **T2.1 `chartFacts.js`**：从 `/chart` 派生/补算——`sign/house`、`sect`（太阳在地平上=昼）、`angularity`（角1/4/7/10 续2/5/8/11 果3/6/9/12，由 `obj.house`）、`combustion`（Cazimi `|lon−sunlon|`<17′、Combust<8.5°、UnderBeams<17°）、`orientality`（东出/西入，由 lon−sunlon 符号；含「出光束=明朗/入光束=停滞」语义）。**全从经度算，零后端。**
- [ ] **T2.2 `conditions.js`**：单星状态 → `{key,value,polarity,weight,text_zh}`：尊贵分（庙+5/旺+4/三分+3/界+2/面+1/陷−5/落−4，读 `obj.dignities`）+ 燃烧 + 逆行(`movedir`) + 东西出 + 速度 + 角续果 + 被夹(读 `surround`) + 游走(读 `isPeregrining`)。
- [ ] **T2.3 `moon.js` + `utils.js`**：月亮 VOC(读 `isVOC`)/燃烧之路(Libra15°–Scorpio15°)/盈亏(日月黄经差)/界末度/夹击/出入相位/**增光=慢减光=快**；`utils.js`：`countSignsBetween(a,b)`（奇偶→件数§7.18/军队大小§7.26）等几何。
- [ ] **T2.4 `aspectsEngine.js` + `reception.js`**：读 `aspects.normalAsp/immediateAsp`（Applicative/Separative/Exact）+ 映点(`antiscias`)；读 `receptions.normal/abnormal`、`mutuals`（区分庙/旺/三分互溶、能否化解凶相，复用 `astroAiSnapshot.js:164-208` 的 `keepReceptionLine`/`hasRulerOrExalt` 思路）。
- [ ] **T2.5 `perfection.js`**：完成 4 法（入相/光线传递/集中接纳/落位）+ 破坏 7 法（阻碍/挫败/折返/无接纳刑冲/燃烧/出相/换座）；以 **Sahl §1.4 三完成法（合相/传递/汇集）为主骨架**，挂 Sibly/Dorotheus 细节；+ 完成度三分（3安全=全成/2=2/3/1=1/3，B.1）+ 难易·努力·主动方·信使发起方（B.2）+ 接收者后续（逆行=事后瓦解，B.3）+ 截断光线链（B.4）。输出命中方式 + 传递星=中间人。
- [ ] **T2.6 `radicality.js`**：卜卦门禁（上升<3°/>27°、月空/燃烧、土落上升受克、上升主燃烧/逆行、月与七宫主刑冲、势均力敌）+ **Hephaistion 一般禁忌合流**（上升主不在 2/8 宫·不合 8 宫主·不刑月；月定位星/福点定位星不在 2/8 宫；"怪物盘"=光体互不合意且与上升不合意+角续全凶无吉注视）+ **痣验证模块（可选）**（上升座/上升主座/六宫头座/六宫主座/月座→部位+行星色+左右[阳右阴左]+前后[地平上前下后]+上中下[度数]，并用 `turnedHouse` 验被问之人）。
- [ ] **验证 M2**：Jest fixture（仿 `liureng/ChuangChart.test.js`）对一张手算盘核对：尊贵分、燃烧判定、角续果、perfection 命中的完成/破坏法、radicality 结论。

---

## M3 — 卜卦完整 `divination/horary/` + 右栏（M2 完成后）

- [ ] **T3.1 `horaryJudgments.js`（规则库）**：对象数组，schema=`{id,source:'sibly|dorotheus|sahl|hephaistion',source_ref,house,category_zh,question_zh,question_en,significators[{role,candidates}],testimonies:{positive[{id,text_zh,weight}],negative[...]},timing_rule,extras}`。录入 **Sibly 71**（主清单 §3.2）+ **Dorotheus 43**（§3.3）+ **Sahl 全部**（补充 §C：含 §10 王国 9 条/§12 赛马·敌人/§13 附加 18 子题[信件 8/谣言/复仇/物品真假/狩猎/宴会/行星小时]/奴隶 §6.4-6.11/贞洁通奸情人 §7.5-7.7/梦境 §9.7）+ **Hephaistion**（III.13 堕胎/III.29 担保/III.37·38 法庭/III.39 放逐弃儿/III.40 监禁/III.46 如何丢失）。**重叠主题（婚姻/盗窃/疾病/旅行/诉讼）同 id 四派证词累加、不互相覆盖**，`weight` 默认 2、主导证词(燃烧/空相/互溶/接纳)调 3。
    - **两条分析骨架（engine 必跑）**：① **Sibly Query I–VI**——每问题套 能否成(perfection)/好坏(事宫强化vs游走逆行出相)/真假(角宫无凶vs空相合南交)/何处(方位距离)/何时(应期)/如何(阻碍助力星=相关人事)。② **Dorotheus 四轴 `fourAxis()`**——择时类用 1=发起/7=对象/10=过程价/4=结果，凶星落某轴=该方有诈；附「角宫=现在/续宫=未来/果宫=过去」。
- [ ] **T3.2 `theftModule.js`**：盗窃/失物 11 步（§3.4：能否找回→位置[4宫元素]→落谁手[4宫星体]→材质[月亮界主星]→外貌[7/9/12宫星或月入相星]→年龄性别[东西出/月相]→是否多人→生熟人[日月注视1宫+转宫]→方向[三套]→逃跑结果[月入相火被抓/土自杀被捕/吉安全]→时间）+ Sahl §7.10-7.22 补充（停滞星=原地被抓[一停戴镣二停逃再抓]、月合逆行=自愿回、月亮界=材质、上升所落**面**→`decanImages`、奇偶→件数）。每步可独立显示。
- [ ] **T3.3 `describe.js`**：`planet_in_sign` **84 条**——从 Sibly 标题「确定当各个星体成为征象星时落在黄道十二星座代表的人物类型」（土/木/火/月/金/水/日 各 12 座）**直抄中文译文**作 `text_zh`，留 `text_en`；+「描述上升定位星」4 条选择逻辑（无相位→只用行星；逆行/落陷→结合座；注视上升→用上升座；两星成相→取更紧/落庙者）。+ thief（§4.2）/配偶（§4.3）/疾病（§4.4）/死亡方式（§4.5）+ Sahl `dream_content`(D.2)/`food_taste`(D.3)/信件发信人身份(D.5)/兽类颜色年龄(D.4)。**按标题定位，勿用硬行号。**
- [ ] **T3.4 `timing.js`**：应期 §5.1（征象星角续果×座模式表 → 度数=数值，南纬延北纬缩）+ 距离 §5.2 + 方位 §5.3（调 `directions.js` 三套）。
- [ ] **T3.5 `horaryEngine.js` + `horarySnapshot.js`**：编排（chart + 问题类别 → 指派征象星[含 `turnedHouse`/`hour_ruler`=`timerStar`] → 跑 engine substrate + testimonies + Query 镜头 → 正负 `weight` 求和倾向 → describe/timing）；snapshot 用 `[小节标题]`+markdown 列表（仿 `guazhan` 的 `buildGuaSnapshotText`）。
- [ ] **T3.6 `components/horary/HoraryJudgment.js`（右栏 6 tab）**：① 起卦（根本性绿/黄+原因、征象星指派）② 状态（各征象星状态卡+月亮大卡）③ 完成（关键相位入/出·吉凶·接纳、命中完成/破坏法、传递星=中间人）④ 裁决（正负两栏列 `text_zh` + 加权倾向：成/不成/势均力敌→建议另择时，**不下命定结论**；正负计数用 `<Tag color='green/volcano'>`）⑤ 时间方位 ⑥ 描述（可折叠）。每条带 `source_ref`「查看出处」。左栏接问题类别 `XQSelect` + 「此刻起卦」按钮。
- [ ] **验证 M3**：用 Sibly **案例 XII–XVI**（盗窃/遗产/舰队/职位晋升/监禁，在「案例 EXAMPLES」段）做 Jest fixture + preview 核对裁决方向/征象星指派一致；标准卜卦盘抽查显示完整。

---

## M4 — 择日完整 `divination/election/` + 右栏（M2 完成后，可与 M3 并行）

- [ ] **T4.1 `modules.js`（13 分析模块）**：按优先级=UI 顺序产 `section{key,title,verdict('good|caution|bad|neutral'),score,findings[{rule_id,severity,polarity,message}],detail_md}`：`moon`→`asc_ruler`→`ascendant`→`sun`→`angles`→`malefic_handling`→`topic_significators`→`topic_house`→`aspect_patterns`→`reception`→`fixed_stars`→`midpoints`→`timing`。**相位格局几何检测**（大三角/风筝/小三角/矩形/梯形/星聚/三刑会冲/大十字，从相位图算；含月/征象星更佳；顶点有主吉相可借动能）。源：择日 §3。
- [ ] **T4.2 `hardFlags.js`（红线）**：§4 约 25 条布尔检查（`moon_void_of_course`[critical]/`moon_square`[high]/`mercury_retro`/`malefic_on_angle`/`saturn_on_angle`/`uranus_on_angle_1_7`/`near_new_or_full_moon`…），带 `*` 者按当前 `topic.must_avoid` 决定触发/升降级。**月相盈亏/近新满月需前端派生**（日月黄经差算盈亏；农历/朔望算近新月廿九~初二、近满月十四~十六）。并入 Hephaistion 2/8 宫禁忌。
- [ ] **T4.3 `scoring.js`**：§5 加权（权重常量 moon .22/asc_ruler .18/ascendant .12/topic_significators .16/angles .14/topic_house .06/sun .05/aspect_patterns .04/reception+fixedstar+midpoint .03）→ `score=clamp(base−penalty,0,100)`（罚分 critical−40/high−15/medium−8/low−3）→ grade（≥85 excellent/70-84 good/55-69 fair/40-54 poor/<40 或含未化解 critical→disqualified）。
- [ ] **T4.4 `rulePacks.js`（27 用事包）**：§6 每包=通用引擎上叠加规则（命中 +/−分 + 文案）：marriage/business/organization/move_in/buy_property/buy_land/renovation/trade/buy_car/deliver_car/contract/registration/application/diet/quit_habit/pursue_love/job_hunt/team_departure/publishing/medication/surgery/banquet/inauguration/funeral/bed_setting/travel/blessing/altar/ritual/general_day。surgery 用 `dorotheusDegrees`/`bodyParts`。**`inauguration` 禁写政治人物名。**
- [ ] **T4.5 `electionEngine.js` + `electionSnapshot.js`**：编排 → `ElectionReport{overall{score,grade,headline,no_perfect_chart_note},hard_flags[],sections[],timing[],recommendations[],chart_facts}`（择日 §1.2）。左栏接 `topicMaster` 用事选择 + 选定后顶部显 `castMoments` 提示 + 时间窗。
- [ ] **T4.6 `components/election/ElectionJudgment.js`（右栏 5 tab）**：① 总评（score+grade+headline+**必带 `no_perfect_chart_note`**+组件条形）② 红线（按 severity 上色）③ 分项（13 sections，月亮在前）④ 应期（§3.13）⑤ 建议（推荐+理由+代价 + 起盘时刻提示）。顶部固定免责文案（§10.1：没有完美择日盘…助力非保证…事在人为+本命>择日）；术语 tooltip 复用 `glossary`。
- [ ] **验证 M4**：选 topic（如 marriage）+ 已知时刻，核 总分/分级/红线/13 分项/建议；明暗双色；§6.16 确认无政治人物名。

---

## M5 — 择日工作流 + 本命结合（M4 完成后）

- [ ] **T5.1 `workflow.js`（消去法 §9）**：定义时间窗+约束 → 去月刑(90°) → 去硬伤(月空/命主用事宫主逆行/相关逆行) → 选月相 → 选月落座 → 各剩余日画正午盘跑 §3 评分 → 微调时刻(只动 Asc/MC，4 分=1°) → 本命裁决 → 给 2–3 候选 + 利弊（推荐+代价）。多候选**并排比较**。
- [ ] **T5.2 `natalIntegration.js`（§7，可选启用）**：提供当事人本命盘时——择日盘=对本命「永久过运」、裁决器、本命昼盘强调木过运/夜盘强调金过运、特别注意土星过运、婚姻 synastry（两造合盘>择日盘）。
- [ ] **验证 M5**：给时间窗扫多候选，出并排比较 + 推荐理由/代价；提供本命盘时裁决器生效。

---

## M6 — AI 接入 + 文案/术语/打磨

- [ ] **T6.1 AI 导出（`utils/aiExport.js` + 两页）**：`AI_EXPORT_TECHNIQUES` 加 `{key:'horary',label:'卜卦盘'}`/`{key:'election',label:'择日盘'}`；`AI_EXPORT_PRESET_SECTIONS` 加 horary（起卦信息/星盘/根本性/征象星/完成分析/裁决/时间方位/描述）、election（起盘信息/星盘/总评/红线/月亮/命主星/命度/征象星/角宫/应期/建议）；按需 `AI_EXPORT_FORBIDDEN_SECTIONS` 加 `['右侧栏目']`。两页盘/判断更新后调 `saveModuleAISnapshot('horary'|'election', buildXxxSnapshotText(...), {})`；如需映射加 `snapshotModuleKeyByContextKey`。
- [ ] **T6.2 文案/术语/描述补齐**：`glossary` tooltip 接入两页；免责文案；描述层补齐（梦境/宴会口味/兽类颜色年龄/信件身份）；`fixedStars` 岁差更新到当前年。
- [ ] **T6.3 可选增强**：① 十二分度(dwad)来意提示——**先查 `components/hellenastro/`「十三分盘」能否复用其十二分度计算**再决定自建；② 值日值时 `planetary_day_hour`（chart 已有 `dayerStar`/`timerStar`）；③ 吉方位（§2.7，依本命盘）。三者均标 optional，时间紧可留到此后。
- [ ] **验证 M6**：AI 助手能选两技法导出、分段正确；术语 tooltip 生效；全页明暗双色 + 窄屏复查。

---

## §V 验证套件（每 milestone 末 + 收尾各跑一次）

1. **单测**：`cd Horosa-Web/astrostudyui && npm test`——`engine/perfection`、`conditions`、`election/hardFlags`、`scoring` 的 fixture 全绿；案例 fixture（Sibly XII–XVI 等）方向一致。
2. **preview MCP**（`horosa-dev` skill 起本地栈）：辅盘→卜卦盘/择日盘；`preview_snapshot`/`preview_inspect`/`preview_screenshot`；**明暗双色 + 窄屏**；左栏改时间→中栏盘变 + 右栏判断刷新；两页时间互不影响、不动主占星盘；卜卦核根本性/征象星/完成/裁决/描述/应期；择日核评分/红线/分项/建议/候选并排；AI 助手导出分段。
3. **收尾发布**：出 `docs/horary-election-v<ver>.md`（架构+复用+验证）+ `docs/windows-sync-handoff.md` 顶部条目（注「纯前端，Windows 随前端同步，无平台特定改动」）；过 `release_preflight.sh` 门禁；**push/tag/publish 前找用户确认**（回帖先草拟经同意再发）。

---

## §附录-1 关键约束复述（写代码时随手核）
- 语义色不动（只动结构/中性间距）｜§6.16 不写政治人物名｜算法自写·译文直抄+`source_ref`｜不下命定结论·势均力敌建议另择时｜量化显式数字｜纯前端零后端｜四派证词并存不覆盖。

## §附录-2 规格 / 源文件（执行全程参照，在 `~/Downloads`）
- 规格：`卜卦占星软件_显示内容构建清单.md`（主：数据/引擎/judgments/describe/timing/render + Sibly71/Doro43 目录）｜`卜卦占星软件_显示内容构建清单_补充Sahl.md`（Sahl §A–§F）｜`择日占星_显示内容实现清单.md`（择日 §0–§12）。
- 源（顶部中文摘要=抽数据，下方双语正文=直抄文案/校对）：`传统卜卦占星问题和案例 分析方法集锦.md`（Sibly：Query I–VI + 12 宫 71 题 + 案例 + 84 描述 + 痣验证）｜`卡门占星 Book V On Interrogations.md`（Dorotheus Ch1–43 + Hephaistion III + Omar 本命三卷[**不做**]）｜`3.On Questions.md`（Sahl §1.1–§13.18）。

## §附录-3 里程碑依赖
M0 →（M1 → M2）→ M3 与 M4 可并行 → M5（依 M4）→ M6。M0–M4 即「完整可用的卜卦+择日断事」；M5–M6 提升专业度。
