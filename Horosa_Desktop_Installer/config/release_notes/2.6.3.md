> v2.6.3 —— AI 分析一轮深度打磨：聊天体验、设置、后端 Provider 矩阵、视觉/用量/JSON 模式都补齐，配套修了一批让用户能感知的 bug。所有技法的命盘计算与 v2.6.2 完全一致。安装旧版的用户启动后自动收到 v2.6.3 更新提示。**本版运行时组件已更新（`2.6.3-runtime1`），升级时会自动下载新运行时。**

- **AI 分析 · 聊天体验升级**：代码块复制按钮+语法高亮（highlight.js）、上滚时暂停自动滚动+「跳到最新」浮动按钮、推理面板流完自动折叠+「复制思考」、对话栏支持拖拽/粘贴图片、「当前挂载」状态条紧贴输入框、首回 AI 完整后**真 AI 生成 6-14 字短标题**（不再截开头废话）、活动对话内直接导出（Markdown / JSON / Word）、错误改为 Alert + 重试按钮、Markdown 渲染补 LaTeX 数学公式（KaTeX）。
- **AI 分析 · 设置与接口**：API Key 改 Password 输入框（眼睛切换+粘贴去空白）、连接 chip 显示延迟与失败详情 Tooltip、Provider「高级」三段折叠（模型/鉴权/请求体）、Provider 列表「卡片/紧凑」视图切换、新 Provider 保存后自动拉模型列表。
- **AI 分析 · 资料 / 模版 / 组合**：资料全 pane 拖拽上传 + 非阻塞批量队列进度条 + 重复文件一次性决策（覆盖/跳过/全部覆盖/全部跳过）、资料卡片 / 列表视图切换、新增文件夹管理 Drawer（CRUD + 「移动到」），模板编辑加变量推断侧栏 + JSON Schema 实时校验，模板版本 diff 弹层，组合包「应用预览」展示对当前案例的影响。
- **AI 分析 · 后端补齐（需重编 jar，已重编）**：用量/费用计量（SSE 新 `usage` 事件，覆盖 OpenAI/Anthropic/Gemini/Ollama 四家，消息底显示 token 与估算成本）；Gemini 视觉补齐（`inlineData/fileData`）；Anthropic/Gemini/Ollama 停止序列 / JSON 模式 / 思考档**显式映射**而非透传；`isReasoningModel` 前后端同步覆盖 gpt-5/6/7、o1/3/4/5/6/7。
- **AI 分析 · 历史 tab 重做**：表头紧凑、列宽固定、操作收成「导出 ▾」下拉；按条件渲染独立空态（不再让单条数据被推到 body 中央）。
- **导出/挂载 · 七政四余补「政余格局/相位」**：右栏 Moira 面板已显示但此前未导出/挂载的两段补齐（`AI_EXPORT_SETTINGS_VERSION` 22→23 自动迁移）。
- **导出/挂载 · 五兆/太玄筮法/荆诀/神易数补 AI 挂载**：四种确定性起卦术此前已可存事盘却挂不上，本版补齐。
- **修复 · 「分至」星盘样式按钮失效**：根因是 `<JieQiChartsMain>` 与内层 `<AstroChartMain>` 漏传 `chartStyle`/`dispatch`，导致样式切换为空操作。补两处 props 即修。
- **修复 · 多处稳定性问题**：消除 Tauri 桌面壳下 `window.prompt/confirm/alert` 引发的「Unhandled Rejection: prompt() is not supported」崩溃（全部 7 处改用 Modal 异步替代）；修若干 Dropdown `overlay={JSX}` 导致的「state update on unmounted」内存泄漏；资料批量导入末段卸载安全；模板版本 diff 空版本兜底；图片上传 onerror + 10MB/张上限。

---

> v2.6.3 — a deep polish pass on AI Analysis: chat experience, settings, backend provider matrix, vision/usage/JSON mode all rounded out, with a wave of user-visible bug fixes. Every technique's chart computation is identical to v2.6.2. Users on older builds receive v2.6.3 automatically on next launch. **The runtime components are updated this release (`2.6.3-runtime1`) and download automatically on upgrade.**

- **AI Analysis — chat UX**: code-block copy button + syntax highlighting (highlight.js), auto-scroll pause on user scroll + floating "jump to latest", auto-collapse reasoning when stream finishes + "copy thinking" action, drag/paste images straight into the composer, "current mount" status bar above the input, first-reply **true AI-generated 6-14 char short title** (no more truncating intro fluff), in-conversation export (Markdown / JSON / Word), errors shown as Alert with a Retry button, Markdown rendering now includes LaTeX math (KaTeX).
- **AI Analysis — settings & providers**: API Key uses Password input (reveal eye + auto-trims pasted whitespace), connection chip surfaces latency and failure detail in a tooltip, provider "advanced" split into three collapsible groups (models / auth / body), provider list switch between card and dense table views, model list auto-fetches when a new provider is saved.
- **AI Analysis — materials / templates / bundles**: full-pane drag-and-drop material upload with a non-blocking batch queue progress bar and one-time decision for duplicates (overwrite / skip / overwrite all / skip all), grid/list switch for materials, new folder management drawer (CRUD + "move to…"), template editor gains a variable inference sidebar plus live JSON Schema validation, template version diff modal, bundle "apply preview" showing the effect on the current case.
- **AI Analysis — backend (jar rebuilt)**: usage/cost metering via a new `usage` SSE event across OpenAI/Anthropic/Gemini/Ollama (token counts and estimated cost shown under each message); full Gemini vision support (`inlineData`/`fileData`); Anthropic/Gemini/Ollama get **explicit** stop sequences / JSON mode / thinking-level mapping instead of generic pass-through; `isReasoningModel` syncs front- and back-end to cover gpt-5/6/7 and o1/3/4/5/6/7.
- **AI Analysis — history tab rework**: compact header, fixed column widths, actions consolidated into an "Export ▾" dropdown; empty state is rendered by an external conditional (no more single-row drifting to the middle of the body).
- **Export/Mount — Qizheng Siyu gains "patterns" and "aspects" sections**: the two right-side Moira panels that were displayed but never exported/mounted are now included (`AI_EXPORT_SETTINGS_VERSION` 22→23 with auto migration).
- **Export/Mount — Wuzhao / Taixuan / Jingjue / Shenyishu now mountable**: these four deterministic divination methods were already storable but couldn't be mounted into AI analysis; this release closes that gap.
- **Fix — "Fenzhi" chart style buttons did nothing**: root cause was the inner `<AstroChartMain>` not receiving `chartStyle`/`dispatch` from `<JieQiChartsMain>`. Two prop wires restore the buttons.
- **Fix — multiple stability issues**: removed the "Unhandled Rejection: prompt() is not supported" crash under the Tauri desktop shell (replaced all 7 native `window.prompt/confirm/alert` calls with async Modal-based dialogs); fixed several `<Dropdown overlay={JSX}>` driven "state update on unmounted" memory leaks; ingest-files batch is unmount-safe; template version diff falls back gracefully when a version is missing; image upload now handles read errors and caps at 10 MB per image.
