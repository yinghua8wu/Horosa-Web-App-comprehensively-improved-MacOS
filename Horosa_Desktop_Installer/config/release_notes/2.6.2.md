> v2.6.2 —— 收尾用户反馈的 issue：紫微 AI 挂载补**宫干自化**、修复部分 Mac 上的「java 运行时找不到」误报。所有技法的命盘计算与 v2.6.1 完全一致。安装旧版的用户启动后自动收到 v2.6.2 更新提示。**本版运行时组件已更新（`2.6.2-runtime1`），升级时会自动下载新运行时。**

- **紫微 · AI 挂载补宫干自化**：宫位总览里每颗星现在会标出「自化禄 / 权 / 科 / 忌」（飞星核心，按所选四化流派自动取值）；生年四化、流年 / 大限 / 流月等运限挂载此前已具备，一并同步到 AI 导出 / 导出设置 / AI 挂载 / 命盘事盘储存。
- **修复 · 部分 Mac「java 运行时找不到」误报**：少数未安装 Xcode 命令行工具的 Mac 上，启动时的 java 探测会误判而报「java runtime not found / 后端启动失败」，实际内置 java 完好。本版改为直接探测 java、去掉多余依赖，未装命令行工具也能正常启动。

---

> v2.6.2 — wrapping up reported issues: Zi Wei AI-mount now includes **palace-stem self-transformation (自化)**, and a false "java runtime not found" error on some Macs is fixed. Every technique's chart computation is identical to v2.6.1. Users on older builds receive v2.6.2 automatically on next launch. **The runtime components are updated this release (`2.6.2-runtime1`) and download automatically on upgrade.**

- **Zi Wei — AI mount adds palace-stem self-transformation**: each star in the palace overview now shows its 自化 (self-transformation of 禄/权/科/忌, a core flying-star reading, derived from the palace's own stem under your chosen 四化 school). Birth-year transformations and annual/major-period/monthly mount layers were already present; all of this syncs to AI export / export settings / AI mount / chart storage.
- **Fix — false "java runtime not found" on some Macs**: on Macs without Xcode Command Line Tools, the startup java probe could misfire and report "java runtime not found / backend start failed" even though the bundled java was perfectly fine. The probe now checks java directly with no extra dependency, so the app starts even without the Command Line Tools.
