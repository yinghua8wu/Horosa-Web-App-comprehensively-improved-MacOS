- **占星各盘符号几何居中**:占星(清简/经典)、合盘、辅盘、主限法盘等所有西洋星盘的星座/行星/度数符号过去整体向上偏约 8px,现已全部落在圆环几何中心(`dominant-baseline` 由 middle 改 central)。
- **太乙四柱随时间基准 + 同时显示两时间**:太乙页同时列出「直接时间」与「真太阳时」(两者稳定不随基准变);选「真太阳时/直接时间」时四柱按对应时刻重算(此前盘变四柱不变)。做法与八字一致。
- **主限法增强**:新增「推运年数」工具条(默认 100,可输入/步进,上限 180);表头压低、底部页码下移、推运方法去掉「该标签」字样。
- **本地 Ollama 不再中途「停止生成」**:AI 分析流式取消写死的 120 秒上限,慢速本地模型(Ollama 等)可完整输出(对应 Windows #6)。
- **其它 UI 修复**:紫微右栏空白区缩短、三合盘神煞字体变细、合盘星盘选择条适配明亮模式、配置面板「当地时间」行在最小窗口不再被裁、顶部菜单精简(去重复的搜索/历史/收藏)。
- **历法**:kinwuzhao/kinastro/kintaiyi 月柱按精确交节判定(日级 → 节气级)。

---

- **Geometric centering of all occidental chart glyphs**: in astrology (清简/经典), composite, auxiliary and primary-direction charts the sign/planet/degree glyphs used to sit ~8px above the ring's geometric center; they are now correctly centered (`dominant-baseline` middle → central).
- **Taiyi four pillars follow the time basis + both times shown**: the Taiyi page now lists both 直接时间 (clock) and 真太阳时 (apparent solar) as stable values, and recomputes the four pillars from the selected basis (previously the board changed but the pillars did not). Same contract as BaZi.
- **Primary Direction**: new 推运年数 (progression years) control (default 100, input/stepper, capped at 180); shorter table header, lower pagination bar, and the "参考" label removed from the method name.
- **Local Ollama no longer stops mid-stream**: AI analysis streaming drops the hard 120s ceiling so slow local models (Ollama, etc.) can finish (Windows issue #6).
- **Other UI fixes**: trimmed Ziwei right-panel blank area, thinner Sanhe shensha glyphs, light-mode adaptation of the composite chart selectors, the 当地时间 row no longer clipped at the minimum window width, and a slimmer top menu (removed duplicate 搜索/历史/收藏).
- **Calendar**: kinwuzhao/kinastro/kintaiyi month-pillar now uses precise 节气 boundaries (day-level → solar-term level).
