- **西占推运补全(7 技法,均接入 AI 分析与 AI 导出)**:
  - **波斯向运(Persian directions)**:主限/向运双圈盘,内圈本命、外圈方向位。
  - **行星弧(Planetary Arc)**、**Vedic 推进**、**Jayne 赤纬向运**、**行星年龄(Planetary Ages)**、**真 129 年法**。
  - **Balbillus**:以 6 张 core 盘校准旺距削减口径与层级单位。
  - **相位美化**、**映点/反映点(antiscia)恢复**。
- **新增:福点整宫制(Part-of-Fortune whole-sign)全局宫制**。
- **新增:时区/夏令时(DST)自动校正**:三处表单(出生/命盘/事盘)按经纬度离线求 IANA 时区并自动校正夏令时。
- **金口诀解读层**:神煞判语、四位生克、应期、分类用神、刑冲合害破、太玄数。
- **七政四余(Moira 还原度)**:28 宿环、显示选项、多 tab、大限、神煞判语。
- **紫微斗数·运限深化**:新增「运限」「格局」两 Tab——八字大运式级联运限(大限▸流年▸流月▸流日▸流时,流年/小限同级)、多层四化卡叠加、格局自动识别;后端 `/ziwei/birth` 返回命中格局,新增 `/ziwei/luck`。
- **六壬·起课法增强(Phase4)**:七政接入 + 起课法(八客/四柱对齐/选时/演数)、换将(中气/节气)、分昼夜(纯前端 castOverride,中心盘与断辞同源)。
- **启动机制稳健化(根治「端口被占用→用不了」「打开后显示后端未启动」)**:端口被占用时先回收自家僵尸、再换一对全新空闲端口自动重试;卡死的自家后端经命令行签名核实后精准回收(绝不误杀第三方进程);就绪前预热排盘服务,避免第一次排盘打到冷服务;排盘请求遇连接中断自动重试;后端掉线时顶部非阻塞「正在重连」横幅。
- 本版含共享后端(Java / Python)改动,已重编 `astrostudyboot.jar`。建议已安装旧版的用户更新到本版。

---

- **New: Western progression / direction techniques (7 methods, all wired into AI analysis & AI export)**:
  - **Persian directions** — double-wheel chart, natal inner ring + directed outer ring.
  - **Planetary arc**, **Vedic progressions**, **Jayne declination directions**, **planetary ages**, **true-129-year**.
  - **Balbillus**, calibrated against six core gold-standard charts.
  - **Aspect-rendering polish** and **antiscia / contra-antiscia** restored.
- **New: Part-of-Fortune whole-sign house system** (global).
- **New: automatic timezone / DST correction** — all three forms resolve the IANA zone from latitude/longitude offline and auto-correct daylight saving.
- **Jin Kou Jue interpretation layer**: shensha verdicts, four-position generation/control, timing, categorized use-gods, xing/chong/he/hai/po, Taixuan numbers.
- **Qi Zheng (Moira fidelity)**: 28-mansion ring, display options, multi-tab, major periods, shensha verdicts.
- **Zi Wei Dou Shu — progression depth**: new "运限/格局" tabs — BaZi-style cascading periods (major ▸ annual ▸ monthly ▸ daily ▸ hourly; annual/minor on the same tier), stacked four-transformation cards, automatic pattern (格局) detection; backend `/ziwei/birth` now returns matched patterns, plus a new `/ziwei/luck`.
- **Liu Ren — casting methods (Phase 4)**: Qi Zheng integration + casting methods (八客 / four-pillar alignment / chosen-hour / derived-number), general-swap (mid-/solar-term), and day/night division.
- **Hardened local-service launch (fixes "port already in use" and "backend not started")**: reclaims our own stale port holder, then auto-retries on a fresh pair of ports; precisely reclaims a wedged own backend after command-line signature verification (never kills third-party processes); warms the chart service before navigation so the first chart never hits a cold service; auto-retries chart requests on a dropped connection; shows a non-blocking reconnect banner if the backend goes away.
- This release includes shared backend (Java / Python) changes and rebundles `astrostudyboot.jar`. Users on older versions are advised to update.
