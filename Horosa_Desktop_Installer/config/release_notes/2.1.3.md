- **八字时间显示修复**:八字盘的「直接时间」与「真太阳时」两者都恒定显示,切换「时间算法」不再跳动(只有「计算基准」随之变化);出生经度偏离北京 +08:00 的 120°E 子午线时,真太阳时按真实日照差与直接时间呈现不同值。
- **Bazi time-display fix**: the chart shows both 直接时间 (clock time) and 真太阳时 (true solar time) as stable values that no longer jump when toggling the time algorithm (only the calculation basis changes); off the +08:00 (120°E) meridian, true-solar differs from clock time by the real solar offset.
