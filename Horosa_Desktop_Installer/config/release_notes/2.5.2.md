- **新增「90°中点盘」技法**（辅盘 → 量化盘）：折叠盘与多环模数盘双形态、三层叠盘（本命/行运/太阳弧）、八虚星（Cu/Ha/Ze/Kr/Ap/Ad/Vu/Po）、谐波盘基 H1..H512、可拖动的红色指针即时输出星体/中点/行星图读数与中点树。行运/太阳弧时刻与地点可独立调节，本命环锁定为最内圈。
- **主限法表格新增 Naibod 度数换算选项**：既有 Ptolemy / Alcabitius 链路完全不变（逐字节对拍通过）；选 Naibod 时表格按 0.9856473 缩放日期、盘投射同步缩放、converse 复用同一参数链。
- **主限法盘四角赤纬归正（A2）**：修复 ASC / MC / DESC / IC 长期误以地理纬度作黄纬导致的赤纬越界（>23.5°）；现一律走 `_pdChartEqCoords(lon, 0.0, obliquity)`，赤经赤纬一致正确。
- **AI 分析：聊天 / 嵌入模型解耦 + 高级参数**：顶栏可独立指定聊天与嵌入模型，「参数」抽屉提供思考档位 / temperature / top_p；技法 Select 上移至「挂载」前。
- **Ollama 本地推理：聊天 + 嵌入均改走原生口**（修 Windows #15）：`/api/chat` 与 `/api/embed`，`num_ctx / num_predict / top_k / top_p / repeat_penalty` 嵌进 `options:{}`，原生口才真正生效；长玄学上下文不再默认 4096 截断；其它 provider 完全不变。
- **大六壬 / 三式合一 AI 挂载修复**：原传 null chartObj 导致 `buildLiuRengLayout` 返 null 无法挂载；现传完整 result.liureng + /chart 合并。
- **六爻接入时间起卦白名单**：仍守「永不按时间重算已存卦」铁律，新增时间入口仅对「新时点」起卦。
- **量化盘 AI 四同步**：AI 导出预设、导出设置版本（14→15）、AI 分析挂载、命盘/事盘储存全部接入新增的 `[90°中点盘]` 段。
- **中点盘 UI 收尾**（用户验收口径）：距离标签由希腊符 Δ 改为短横线 `-`、TNP 关时指针读数和中点树同步隐藏、行运/太阳弧支持独立地点覆盖（空=同本命）、左右栏布局优化避免截断与换行、saKey（Naibod / 1°）刷新页面持久化。

---

- **New "90° midpoint dial" technique** (Aux → Quantitative): folded and multi-ring modulus forms, three stacked rings (natal / transit / solar arc), eight hypothetical points (Cu / Ha / Ze / Kr / Ap / Ad / Vu / Po), harmonic bases H1..H512, and a draggable red pointer that emits live planet / midpoint / planetary-picture readouts plus a midpoint tree. Transit / solar-arc time and location are independently adjustable; the natal ring is locked as the innermost.
- **Naibod added to the primary-direction table**: the verified Ptolemy / Alcabitius paths stay byte-identical (golden-tested); with Naibod selected the table scales the date by 0.9856473, the chart projection scales accordingly, and converse reuses the same parameter chain.
- **Primary-direction-chart four-angle declination fix (A2)**: ASC / MC / DESC / IC no longer treat geographic latitude as ecliptic latitude (which had been producing declinations beyond 23.5°); all four angles now go through `_pdChartEqCoords(lon, 0.0, obliquity)` so right ascension and declination are coherent.
- **AI Analysis: chat / embedding model decoupling + advanced parameters**: the top bar lets you pick chat and embedding models independently, and a "Parameters" drawer exposes thinking level / temperature / top_p; the technique Select moves above the Mount step.
- **Ollama (local inference): chat + embedding both switched to native endpoints** (fixes Windows #15): `/api/chat` and `/api/embed`, with `num_ctx / num_predict / top_k / top_p / repeat_penalty` nested under `options:{}` — only then does Ollama actually honour them; long astrology context no longer truncates at the default 4096. Other providers are completely unaffected.
- **Da-Liu-Ren / Three-style-united AI mount fix**: previously the snapshot was built from a null `chartObj` so `buildLiuRengLayout` returned null and the mount stayed empty; now the full `result.liureng` is merged with the `/chart` payload before snapshotting.
- **Liu-yao added to the time-cast whitelist**: still honours the "never recast saved hexagrams from time alone" rule; the new time entry only casts a fresh hexagram for a new timepoint.
- **Midpoint-dial AI four-way sync**: the AI export presets, export-settings version (14→15), AI analysis mount, and chart / case storage all carry the new `[90°中点盘]` section.
- **Dial UI polish** (per user acceptance): distance labels use a hyphen `-` instead of the Greek Δ; when TNP is off the pointer readout and midpoint tree hide TNP entries; transit / solar-arc support independent location overrides (empty = same as natal); left/right panel layout avoids truncation and wrapping; the solar-arc key (Naibod / 1° per year) persists across reloads.
