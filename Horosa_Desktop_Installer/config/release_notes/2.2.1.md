- **全局设置 · 日界点 + 晚子时·时柱起干**：「设置 → 全局设置」拍板两个独立开关，覆盖 `23:00–23:59` 区间的四柱排盘。
  - **日柱开关**：「23点算第二天（默认）」 / 「24点算第二天」 —— 控制 hour ∈ [23, 24) 时日柱是否进位次日。
  - **时柱开关**：「晚子时按次日日柱计算（默认）」 / 「晚子时按当日柱计算」 —— 控制起子时时干用今日 vs 次日的日干。
  - 27 日 23:30 自检矩阵：`(日,时)=(1,1)` 壬寅 庚子｜`(1,0)` 壬寅 **戊子**｜`(0,1)` 辛丑 庚子｜`(0,0)` 辛丑 **戊子**（两个核心新 case）。
- **覆盖全技法且四柱一致**：八字 / 紫微 / 六壬 / 六爻 / 奇门 / 太乙 / 三式 / 七政（Moira）/ 金口诀 / 印度盘 全部受这两开关支配；命盘 / 案例存储均持久化两值，重新打开后回填一致。本版修齐 Moira / 金口诀 / 太乙 切换后需刷新才生效的 bug，并修正 `(1,0)` 边界在 Java 系技法（八字/六壬/金口/七政）原返「庚子」与钉定矩阵「戊子」不一致的问题（`BaZiHelper` 四条时柱路径对称补全）。
- **软件内升级 · 非阻塞**：进入软件自动检测新版 → 右下角非模态卡片让你选「更新 / 稍后」→ 选择更新后**后台下载、可最小化为进度药丸，不影响正常使用** → 下载完成后由你主动「重启更新」。「设置 → 关于星阙 / 检查更新」可随时手动检查。
- **偏好设置全面精修**：全局设置 / AI导出设置 / 排盘设置 / 星盘参数 / AI分析设置 等弹窗统一为精致一致的桌面端风格（分区、说明卡、间距、亮/暗双主题），术数语义色保持不变。
- **AI 供应商连通性修复**：① Anthropic（含第三方中转）对话/测试连接因 content 块缺 `type` 报 503 已修；② 未登录/未配置凭据的裸 401 改为可操作的中文提示；③ HTTP 客户端在无代理环境变量时回退读取系统代理属性，缓解「本地代理正常但连不上」。
- **AI 分析挂载排盘规则**：导出快照在四柱前置 `排盘规则：日柱开关【…】+ 时柱开关【…】。本盘四柱按此规则计算。` —— GPT/Claude/Ollama 无须再凭默认语义猜测，避免 AI 解读与 UI 显示对不上号。
- **Java 后端 `ChartController.getParams()` 白名单加固**：上一版同型 bug 一并修复 —— 任何 chart-flow 新字段必须显式 `params.put(...)`，否则后端永远拿默认值。本版同步审计了所有 `getParams()`-style controllers。
- **重编 `astrostudyboot.jar`** + Java 进程重启（`BaZiHelper` / `BaZi` / `NongliHelper` / `OnlyFourColumns` / `ChartController` 都改了）。
- **配套文档**：`Horosa-Web/docs/global-day-boundary-v2.2.1.md` + `docs/windows-sync-handoff.md` 顶部条目。
- **AI 分析 SSE 流双修复（Windows Issue #8）**：`AIAnalysisProxyService.java` 两处加固。① catch 块第一步必须 `QueueLog.error(AppLoggers.ErrorLogger, e)` 记一级异常，`sendEvent` / `completeWithError` 各自嵌套 `try` —— 修了之前"上游 Ollama 抛错，catch 给前端发 error 又撞 `ClientAbortException` → `RuntimeException` 把 `ai-analysis-chat-stream` 线程炸了，原始一级异常被吞掉根因黑盒"的可观测性 bug。② `streamOpenAICompatible` / `streamAnthropic` / `streamGemini` 三处包一层 `withHeartbeat` —— 每 15s 给 emitter 发 `: keep-alive` 注释帧，防止 Ollama 慢首 token（本地 10–60s）时客户端/Chromium/中间件按空闲超时切断 SSE。`release_preflight.sh` 加哨兵 7，任何回退均拒发。

---

- **Global setting · day boundary + late-zi-hour pillar mode**: the Settings → Global panel now lets users pin two independent switches that govern the four-pillar layout for the `23:00–23:59` window.
  - **Day-pillar switch**: "23 starts the next day (default)" vs "24 starts the next day" — controls whether the day pillar advances at 23:00.
  - **Hour-pillar switch**: "late-zi hour follows next-day stem (default)" vs "late-zi hour follows today's stem" — controls which day stem seeds the hour stem at 23:00.
  - Self-check matrix for 2026-05-27 23:30: `(day,hour)=(1,1)` 壬寅 庚子｜`(1,0)` 壬寅 **戊子**｜`(0,1)` 辛丑 庚子｜`(0,0)` 辛丑 **戊子** (two new cases).
- **Applies to every technique, pillars consistent**: BaZi / Ziwei / Daliuren / Six-Yao / Qimen / Taiyi / Sanshi / Qizheng (Moira) / Jinkou / Indian chart all honor both switches; saved charts and cases persist and reload both values. This release fixes the "needs a reload to take effect" bug on Moira / Jinkou / Taiyi, and the `(1,0)` boundary inconsistency where Java techniques (BaZi/Liuren/Jinkou/Qizheng) returned 庚子 instead of the pinned 戊子 (`BaZiHelper` four hour-stem paths symmetrically completed).
- **In-app update · non-blocking**: the app auto-checks on launch and shows a non-modal bottom-right card (Update / Later); choosing Update downloads **in the background, minimizable to a progress pill, without blocking normal use**, then you restart on your own when ready. Settings → About / Check for updates lets you check manually anytime.
- **Preferences UI overhaul**: the Global / AI-export / chart / params / AI-analysis settings dialogs are unified into a polished, consistent desktop look (sections, hint cards, spacing, light/dark) with the divination semantic colors preserved.
- **AI provider connectivity fixes**: ① Anthropic (incl. relay) chat/test-connection 503 caused by content blocks missing `type` is fixed; ② the raw 401 for unauthenticated/unconfigured credentials becomes an actionable message; ③ the HTTP client falls back to system proxy properties when no proxy env var is set.
- **AI snapshot now declares the active rule**: the export carries a leading `排盘规则: 日柱开关【…】+ 时柱开关【…】。本盘四柱按此规则计算。` line so GPT/Claude/Ollama no longer have to guess default semantics, eliminating the "AI answer disagrees with chart on screen" class of bug.
- **Java `ChartController.getParams()` whitelist hardening**: same shape as the prior fix — any new chart-flow field must be explicitly added to the whitelist or the backend silently falls back to defaults. This release audits every `getParams()`-style controller.
- **Rebundled `astrostudyboot.jar`** + Java-process restart (`BaZiHelper` / `BaZi` / `NongliHelper` / `OnlyFourColumns` / `ChartController` all changed).
- **Companion docs**: `Horosa-Web/docs/global-day-boundary-v2.2.1.md` + `docs/windows-sync-handoff.md` top entry.
- **AI-analysis SSE stream — two-part fix (Windows Issue #8)**: `AIAnalysisProxyService.java`. ① The `chatStream` catch block now logs the original exception via `QueueLog.error(AppLoggers.ErrorLogger, e)` BEFORE anything else, and wraps `sendEvent` / `completeWithError` in nested try-catches — previously, when Ollama threw upstream, the catch's `sendEvent` ran into `ClientAbortException` and rethrew a `RuntimeException` that killed the `ai-analysis-chat-stream` thread while the real first-cause exception was swallowed by `safeErrorMessage`. ② `streamOpenAICompatible` / `streamAnthropic` / `streamGemini` are now wrapped in `withHeartbeat`, which emits a `: keep-alive` SSE comment every 15 s so that browsers/Chromium/middleware do not time the connection out while a local Ollama model is taking 10–60 s to produce its first token. `release_preflight.sh` adds sentinel 7 to block any regression.
