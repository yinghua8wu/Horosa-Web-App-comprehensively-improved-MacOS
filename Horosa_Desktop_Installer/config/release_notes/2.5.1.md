- **AI 分析页系统性翻新（面向用户）**：对话改为主流 Chat 式——空态居中气泡输入框、出文展开为限宽居中消息流、占满竖向；顶栏三步「配置 → 测试连接 → 对话」，技法/资料/系统提示/挂载预览收进「挂载设置」抽屉。
- **AI 挂载正确性修复**：换命盘/事盘后挂载内容严格对应所选盘（严格签名匹配，杜绝挂出「上次看过的那张盘/卦」）。
- **技法全面接入 AI 挂载/导出**：西占全套推运（主限法盘 / 黄道星释 / 界推运 / 小限 / 太阳弧 / 太阳·月亮返照 / 流年 / 十年大运 等）、数算（邵子参评数 / 河洛理数，**补齐逐年流年卦/岁运**）、演禽 / 策天飞星 / 皇极经世，均可挂载与导出；导出设置可逐技法逐段勾选。
- **新增「起课时间」「命盘时间」两个即时起盘入口**：仅凭时间 + 地点（默认此刻，可改）即时起盘——起课时间对六壬 / 金口诀 / 奇门 / 太乙 / 三式合一 / **卜卦盘 / 择日盘**，命盘时间对八字 / 紫微 / 星盘 / 各推运；两入口内置完整设置（地图 atlas 选点 / 时区 / 命名 / 性别 / **一键保存为命盘·事盘**）。六爻 / 统摄法等摇卦类**永不按时间凭空起**。
- **AI 对话每条回复**新增「复制全文」「重新生成」；**测试连接**做成状态机（切换模型后重置，测试成功显绿「测试成功」）。
- **地点选择内置成熟 atlas**：全球 3.4 万城市离线库（支持中文 / 英文搜索）+ 城市数据集 + 地图选点 + 手输经纬；坐标按标准度分秒（116°24′27″E）显示；**时区可自动推断也可手动微调，并随命盘/事盘存档、贯穿 AI 挂载/分析/导出**。
- **七政四余（Moira 模式）**：星体居中于宿环两线之间、过近自动错开并用连线标度数；庙旺标注按「庙/旺」如实显示。
- **紫微斗数**：修复右栏信息面板底部空白；修复点击宫位后本命神煞被误改（本命神煞恒定不变）。
- **天文馆**：「全屏」改为沉浸模式（隐藏两侧栏、画布铺满，带精致的浮动「退出沉浸」按钮与动画）；左栏收合按钮改为简洁箭头。
- 占星页移除失效的「收合」按钮。
- 本版为**纯前端更新**，沿用既有后端，安装即生效。

---

- **AI Analysis page — full UX overhaul**: the chat is now mainstream-style — empty state shows a centered bubble input, expands into a width-bounded centered message stream filling the space; a three-step top bar (configure → test connection → chat); techniques / materials / system prompt / mount preview collapse into a "Mount settings" drawer.
- **AI mount correctness fix**: after switching chart/case, the mounted content strictly matches the selected chart (strict signature match — no more mounting "the last chart/hexagram you viewed").
- **All techniques wired into AI mount / export**: the full Western progression set (primary directions chart / zodiacal releasing / distributions / profections / solar arc / solar & lunar returns / given-year / decennials, etc.), numerology (Shao-zi Can-ping / He-luo Li-shu — **annual hexagrams across the life now included**), Yan-qin / Ce-tian / Huang-ji — all mountable and exportable, with per-technique per-section toggles in export settings.
- **Two new instant-cast entries — "Cast time" and "Natal time"**: cast a chart from just time + location (default now, editable) — Cast time covers Liu-ren / Jin-kou / Qi-men / Tai-yi / unified three-styles / **horary / electional**, Natal time covers BaZi / Zi-wei / Western chart / progressions; both embed full settings (map atlas picker / timezone / name / gender / **one-click save as chart·case**). Coin/number-cast methods (Liu-yao etc.) are **never fabricated from time alone**.
- Each AI reply now has **Copy full text** and **Regenerate**; **Test connection** is now a proper state machine (resets on model switch, turns green "Test OK" on success).
- **Mature built-in location atlas**: offline DB of 34k+ world cities (Chinese / English search) + dataset + map picker + manual lat/lon; coordinates shown in standard DMS (116°24′27″E); **timezone auto-inferred or manually adjustable, saved with the chart/case and carried through AI mount/analysis/export**.
- **Qi-Zheng (Moira mode)**: planets centered between the mansion-ring lines, auto-spread when too close with a degree-leader line; dignity marks show 庙/旺 correctly.
- **Zi-wei**: fixed the blank gap at the bottom of the right info panel; fixed natal shensha being altered on palace click (natal shensha now stays fixed).
- **Planetarium**: "Fullscreen" is now an immersive mode (both side panels hidden, canvas filling, with a polished floating "Exit immersive" button + animation); the left-panel collapse is now a clean arrow.
- Removed a non-functional "collapse" button on the astrology page.
- This release is **frontend-only**, reuses the existing backend, and takes effect on install.
