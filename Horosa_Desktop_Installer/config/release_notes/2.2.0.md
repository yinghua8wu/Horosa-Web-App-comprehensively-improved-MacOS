- **数算新增两大技法（全前端本地推算，不走后端）**：
  - **邵子参评数（金锁银匙）**：古法/明法两种取法可在左栏切换，列本命、大运（命宫顺行）与 1–120 岁全表流年；四柱用星阙自己的节气精确八字。
  - **河洛理数**：八字 → 天地数 → 先天/后天卦 + 元堂（含三至尊卦换卦特例），起大限、流年、并可逐级下钻流月→流日；每卦附纳甲六亲世应、互卦/覆卦、含/藏/覆 元气化工「理数」、河洛爻辞与倪海厦易经推命断语；点行看爻辞、点 ▾ 看下级。
- **调波盘绘制**：辅盘·调波盘改用与量化盘一致的完整星盘渲染（按调波数变换 ASC/星曜/宫位/相位），改调波数即时重绘。
- **风水盘 iframe → React 全重写**：与全站统一的 UI/明暗/占屏；上传户型图、框选房屋、纳气盘八方与标记判定、建议汇总均原生重绘，并写入 AI 快照。
- **统一**：两数算技法与上述模块的明暗主题、命盘存储、AI 分析导入与 AI 导出（含分段输出）已全部接入。
- **AI 分析修复**：修正「测试连接 / 模型下拉」把 Embedding 模型（如 `text-embedding-004`）误发到对话端点导致的 404；Gemini 预设补默认聊天模型 `gemini-2.5-flash/2.5-pro`；后端 chat 现拒收 Embedding 模型并回报可读错误。

---

- **Two new 数算 (numerology) techniques — fully local, no backend**:
  - **邵子参评数 (Jinsuo Yinshi)**: switch 古法/明法 in the left panel; shows 本命, 大运 (命宫-forward) and a full 1–120-year 流年 table, all from Horosa's own solar-term-accurate four pillars.
  - **河洛理数 (Heluo Lishu)**: BaZi → heaven/earth numbers → pre/post-heaven hexagrams + 元堂 (incl. the three 至尊 hexagram exceptions); builds 大限, 流年, and drills 流月 → 流日. Each hexagram carries 纳甲 six-relatives/world-response, mutual/reverse hexagrams, the 含/藏/覆 元气-化工 "理数", the Heluo line texts and Ni Hai-Xia Yijing readings. Click a row for its line text, click ▾ to drill down.
- **Harmonic chart drawing**: the auxiliary harmonic chart now uses the full Germany-style wheel (ASC/planets/houses/aspects transformed by the harmonic number) and redraws instantly when the harmonic changes.
- **Feng-shui board iframe → full React rewrite**: unified UI / dark mode / full-screen; floor-plan upload, room selection, eight-direction qi board, marker verdicts and summary all render natively and feed the AI snapshot.
- **Integration**: both 数算 techniques and the modules above are wired into dark mode, chart saving, AI-analysis import and AI export (with segmented output).
- **AI-analysis fix**: the model dropdown / "test connection" no longer mis-sends an embedding model (e.g. `text-embedding-004`) to the chat endpoint (the reported 404); Gemini now ships default chat models `gemini-2.5-flash/2.5-pro`; the backend rejects embedding models for chat with a readable error.
