> v2.6.4 —— 恒星黄道（sidereal）全面补齐：所有西洋占星盘的「黄道」从「回归/恒星」二元扩为 **回归 + 47 种岁差（ayanāṃśa）**，每一种都真实改变盘中星位；西洋盘恒星黄道下新增「月宿（Nakshatra）」；印度占星左栏下拉遮挡修复 + 岁差制/分宫制全补；AI 分析的导出 / 导出设置 / 挂载 / 命盘事盘储存全部同步到位。默认行为与 v2.6.3 完全一致（不选恒星制就跟以前一样）。**本版运行时组件已更新（`2.6.4-runtime1`），升级时自动下载新运行时。**

- **恒星黄道 · 47 种岁差全补**：命盘占星 / 合盘 / 中点盘 / 三维盘 / 卜卦 / 三式 / 节气盘 等所有有「黄道」选项的页面，恒星黄道下现可选 47 种 ayanāṃśa（Lahiri / Raman / Fagan-Bradley / KP / De Luce / Krishnamurti / Galactic Center 等，分组下拉）。**每一种都经 Swiss Ephemeris 真实计算、盘中星位随之位移**；盘圈标注具体岁差名。向后兼容：默认仍是回归黄道，普通「恒星」等同 Lahiri，逐位与旧版一致。
- **西洋盘月宿（Nakshatra）**：选恒星黄道时，行星卡片新增「月宿」行（27 宿 + 宿主 + pada 第几分），与印度盘同口径。回归黄道下不显示。
- **印度占星 · 下拉遮挡修复 + 岁差/分宫全补**：左栏岁差制 / 分宫制等下拉的长标签不再被截断（下拉按内容展宽 + 悬浮显示全文）；岁差制 6 → **47** 种、分宫制 4 → **24** 种（全部走 pyswisseph，零自研数学）。
- **AI 分析 · 双盘技法补「本命盘 + 时段盘」双配置**：太阳返照 / 月亮返照 / 太阳弧 / 小限法 / 流年法 / 行星弧 / 主限法盘 / 恒星(Vedic)推运 / 赤纬(Jayne)推运 等「两盘叠加」技法，AI 挂载与导出此前只有零散数据，现完整输出**本命盘配置 + 选定时段盘配置 + 相位**，AI 解读不再缺料。
- **AI 分析 · 挂载设置全补**：印度占星（岁差制 / 分宫制）、七政四余（宿度制：今制 / 开禧 / 郑式恒星 / 荀爽 / 斗柄）、西洋盘（恒星黄道下可在挂载抽屉覆盖具体岁差）等技法的可调项全部接入「每技法挂载设置」，与命盘页一致。
- **AI 分析 · 波斯向运「应期年数」开关**：右侧表格可选计算 50 / 90 / 120 / 150 / 200 年，不再固定 90 年。
- **AI 导出 · 段同步**：上述双盘新段（本命盘配置 / 时段盘配置 等）进入「导出设置」可勾选项，导出设置版本 23 → 24 自动迁移（已自定义过段的用户无损并入新段）。
- **修复 · 一批跨模块问题**：① AI 命盘快照签名现纳入具体岁差 —— 换 ayanāṃśa 后不再误用上一种岁差的旧快照；② 择日「时势合参」子盘随主盘岁差计算（此前漏传退默认 Lahiri）；③ 主限法盘表单字段保真补全。
- **工程**：黄道/岁差参数贯穿全栈透传补齐（前端各盘 + Java 控制器 + Python 排盘 + 响应回显 + 命盘事盘储存），后端 Java jar 与 Python 排盘组件已重编/更新。

**本版还合并两套此前已完成的功能：**

- **AI 分析 · 报告生成（新）**：八字 / 紫微各 3 档粒度（8 / 12 / 20 节）共 6 套预置模板，分节流式生成（并发 + 空回退重试 + 流派 prompt 注入 + 首末页辅助节），章节可嵌入命盘截图（八字四柱 / 紫微命盘，后端真排盘渲染后截图；失败优雅回退纯文本不崩），9 套流派 guideline（子平 / 盲派 / 新派 / 滴天髓 / 神峰通考 + 北派飞星 / 中州 / 三合 / 钦天），支持 Markdown / Word / PDF / HTML 四种导出（PDF 智能分页防裁字）。聊天栏快捷按钮一键发起。
- **启动健壮性加固（Mac #12）**：排盘服务未就绪时透明重试 6 → 10 次（累计 12s → 30s，覆盖慢机首启窗口）、「排盘失败」改富错误对话框（4 类原因 + 立即重试 / 重启后端 / 打开诊断 / 复制诊断信息）、右下角常驻后端健康灯（🟢🟡🔴 + Popover 详情）、启动页分阶段文案（6s / 15s / 30s）+ Tauri 操作按钮、离线横幅加 3 个操作按钮。

---

> v2.6.4 — Sidereal zodiac, fully built out: every Western chart's "zodiac" selector goes from a binary tropical/sidereal to **tropical + 47 ayanāṃśas**, each of which genuinely shifts the planets in the chart; Western charts gain per-planet **Nakshatra** under a sidereal zodiac; the Indian-astrology side fixes the clipped left-panel dropdowns and adds the full ayanāṃśa/house-system sets; and AI Analysis export / export settings / mount / chart storage are all kept in sync. Default behavior is identical to v2.6.3 (if you don't pick a sidereal mode, nothing changes). **The runtime components are updated this release (`2.6.4-runtime1`) and download automatically on upgrade.**

- **Sidereal — all 47 ayanāṃśas**: natal / synastry / midpoint / 3D / horary / three-systems / solar-term charts — every page with a "zodiac" selector now offers 47 ayanāṃśas under sidereal (Lahiri, Raman, Fagan-Bradley, KP, De Luce, Krishnamurti, Galactic Center, …, grouped dropdown). **Each is computed through Swiss Ephemeris and shifts the chart positions accordingly**; the chart ring labels the active ayanāṃśa. Backward compatible: default stays tropical, plain "sidereal" equals Lahiri, bit-for-bit identical to the old build.
- **Western Nakshatra**: under a sidereal zodiac, each planet card gains a Nakshatra line (27 mansions + lord + pada), matching the Indian chart.
- **Indian astrology — dropdown clipping fix + full ayanāṃśa/houses**: the long ayanāṃśa / house-system labels in the left panel are no longer truncated (dropdown widens to content + hover shows the full label); ayanāṃśa 6 → **47**, house systems 4 → **24**, all via pyswisseph.
- **AI Analysis — dual-chart techniques now output natal + period config**: solar/lunar return, solar arc, profection, given-year, planetary arc, primary directions chart, Vedic and Jayne progressions — these "two-ring" techniques previously mounted/exported only scraps; they now emit the full **natal config + selected-period config + aspects**.
- **AI Analysis — complete mount settings**: Indian astrology (ayanāṃśa / houses), Qizheng Siyu (mansion-degree mode), and Western charts (override the specific ayanāṃśa from the mount drawer under sidereal) all expose their adjustable options in per-technique mount settings.
- **AI Analysis — Persian Directed "years" switch**: choose 50 / 90 / 120 / 150 / 200 years instead of a fixed 90.
- **AI export — section sync**: the new dual-chart sections become toggleable in export settings; export-settings version 23 → 24 migrates losslessly for users who had customized sections.
- **Fixes**: ① the natal AI-snapshot signature now includes the specific ayanāṃśa, so switching ayanāṃśa no longer reuses a stale snapshot; ② electional "time-context" sub-charts follow the main chart's ayanāṃśa (previously fell back to default Lahiri); ③ primary-directions chart form-field fidelity.
- **Engineering**: the zodiac/ayanāṃśa parameter is threaded through the full stack (front-end chart builders + Java controllers + Python chart engine + response echo + chart storage); the Java jar and Python chart components are rebuilt/updated.

**This release also folds in two previously-completed feature sets:**

- **AI Analysis — Report generation (new)**: 6 preset templates (BaZi and ZiWei, each at 8 / 12 / 20-section granularity), section-by-section streaming generation (concurrency + empty-retry + school-specific prompt injection + lead/closing helper sections), chapters can embed chart screenshots (BaZi four-pillars / ZiWei chart, rendered from a real backend chart then captured; graceful text-only fallback on failure, never crashes), 9 school guidelines, and four export formats (Markdown / Word / PDF / HTML, with smart PDF pagination to avoid clipped text). Launchable from a composer shortcut.
- **Startup robustness (Mac #12)**: transparent retry on the local chart service from 6 → 10 attempts (12s → 30s, covering slow first-boot windows), a rich "chart failed" dialog (4 cause categories + retry / restart backend / open diagnostics / copy diagnostics), a persistent backend health dot (🟢🟡🔴 + popover) bottom-right, staged startup messaging (6s / 15s / 30s) with Tauri action buttons, and three action buttons on the offline banner.
