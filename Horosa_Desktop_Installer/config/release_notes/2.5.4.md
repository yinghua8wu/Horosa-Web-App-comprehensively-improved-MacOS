> v2.5.4 —— 主限法（Primary Directions）全面补全 + 启动 / 运行稳健化。安装旧版的用户：启动后自动收到 v2.5.4 更新提示。**默认 Alcabitius + Ptolemy 路径逐行字节级不变**。

- **主限法 · 方位法扩充**：在 Alcabitius 之外新增多种古典方位法，走自研通用引擎——球面三角闭式 + `swisseph.house_pos` 数值法作地面真值，闭式与数值法逐位自检一致。
- **方向类型 In Zodiaco / In Mundo**：黄道向运（弃黄纬、相位为黄道点）与世俗向运（带黄纬、相位在房屋空间），世俗下相位落在房屋空间。
- **向运顺逆可同选、按年龄交错**：顺向（direct，正弧）与逆向（converse，负弧）可同时勾选；同选时两批按推进年龄（日期）升序交错显示。**默认顺逆都开**。
- **度数换算三钥匙**：Ptolemy（1°/年）/ Naibod（0.9856473）/ **真太阳弧（动态：逐弧查星历求真太阳到 natal_sun_ra+arc 的天数，1 天=1 年）**；主限法表格与主限法盘对真太阳弧用互逆的正 / 逆函数，盘表一致。
- **映点 / 界作迫星**：可叠加映点 / 反映点（antiscia）与埃及界（terms）边界作为额外迫星。
- **顶部单行工具栏 + 主限法盘按设置投影**：表格全部选项（方法 / 度数 / 方向类型 / 向运 / 年数 / 映点 / 界）压顶部单行；主限法盘右栏可调方法 / 钥匙 / 向运，外圈按真太阳弧动态投射、converse 反向投影。右栏「主/界限法」tab 更名「主限法」，并删去与「界」重复的「显示界限法」旧选项。
- **AI 四同步**：新主限法设置（方法 / 钥匙 / 方向类型 / 顺逆 / 映点 / 界）接入 AI 导出、导出设置、AI 分析挂载、命盘 / 事盘储存。
- **启动 / 运行稳健化**：前端 StartupGate（连接中覆盖层 + 自动退避重试 + 手动重试 + 重建 Runtime 提示）、后端 `/healthz` 就绪契约、Java 绑 `127.0.0.1`（防火墙友好）与全阶段结构化启动日志，根治「白屏 / 后端未就绪」边缘卡顿。
- **回归守护**：540 例 Alcabitius+Ptolemy 逐字节对拍、pd_engine 球面对拍、前端同步 + AI 上下文自检全绿；preflight 新增主限法 v10 与启动稳健化门禁。

---

> v2.5.4 — primary directions completed, plus a startup / runtime hardening pass. Users on older builds receive v2.5.4 automatically on next launch. **The default Alcabitius + Ptolemy path stays byte-for-byte identical.**

- **Primary directions — expanded house systems**: additional classical methods alongside Alcabitius, on a self-developed engine (spherical-trig closed forms validated against `swisseph.house_pos` as ground truth; closed-form and numerical results agree position-by-position).
- **In Zodiaco / In Mundo direction type**: zodiacal vs mundane directing, with mundane aspects taken in house space.
- **Co-selectable direct + converse, interleaved by age**: direct (positive arc) and converse (negative arc) can both be on; when both are selected the two sets interleave by progressed age (date) ascending. **Both on by default.**
- **Three time keys**: Ptolemy / Naibod / **True Solar Arc** (dynamic, ephemeris-driven), consistent between the table and the dial via mutually-inverse forward / backward functions.
- **Antiscia / terms as promissors**, an all-on-one-row top toolbar, a method-aware primary-direction dial (projects per method / key / direction, reverse projection for converse), the inner tab renamed to "Primary Directions," and the redundant "show term directions" option removed.
- **AI four-way sync** for the new settings (method / key / direction type / direct-converse / antiscia / terms) across export, export settings, analysis mount, and chart / case storage.
- **Startup / runtime hardening**: a front-end StartupGate (connecting overlay + auto-backoff retry + manual retry + rebuild-runtime CTA), a backend `/healthz` readiness contract, Java bound to `127.0.0.1`, and structured startup logs.
- **Regression guards**: 540-case byte-perfect Alcabitius+Ptolemy diff, pd_engine spherical cross-checks, front-end sync + AI-context self-checks all green; preflight gates extended for primary-direction v10 and startup hardening.
