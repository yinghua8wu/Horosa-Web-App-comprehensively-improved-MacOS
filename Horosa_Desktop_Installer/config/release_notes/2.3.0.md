- **占星地图（地理占星 / ACG）全面升级**：辅盘「占星地图」由旧的 AMap + 迭代搜索重写为 **D3-geo + 内置 GeoJSON + 解析法赤经赤纬四轴线**。世界地图用内置 Natural Earth 110m 开放数据；ASC/DSC 升落曲线、MC/IC 子午线、赤道/黄道/回归线、Parans 全部按解析公式精确绘制并多点采样（修正全宽水平线、极区钩线断裂、子午线缺口等渲染陷阱）。新增落点分析（迁移四轴 + 解读）、本地空间、Parans（关/日月/全部）、4 种底图样式，明暗主题随系统即时切换。算法以 `swisseph.azalt()`独立回归自检（`validate_acg.py` → PASS）。
- **辅盘新增「卜卦盘 + 择日盘」**：西洋占卜（Horary）断事规则库 + 择日（Election）盘，右栏排版美化，事件盘可存盘/重开，并接入 AI 分析挂载。
- **数算 · 河洛理数补全**：① **取化工法**做成该技法**左栏选项**（土王寄坤艮 / 直取四方伯，镜像参评数古法/明法）；② 运盘流年/流月行显示**精确节气交时**（立春 / 月建节）；③ 命运篇补全 5 项依古籍可算的判语 —— 阴阳二数命名（二十四数名）、众宗/众疾、顺/反数+季节宜、正/反对体凶警示、看命大法命格综合评分。主链算法零改、对古籍原文逐条审计。
- **多项修复与打磨**：AI 模型代理连通性加固（Java + 启动器）、六壬「发三传」顺序按典籍修正、占星右栏偶发空白块修复、风水模块 UI 重写、快捷功能 dock 自适应。
- 本版含 Java 后端改动（占星地图 / AI 代理），已重编 `astrostudyboot.jar`；其余为纯前端。

---

- **Astrocartography (ACG) overhaul**: the auxiliary "Astro Map" is rebuilt from the old AMap + iterative search to **D3-geo + bundled GeoJSON + analytic RA/Dec four-axis lines**. The world map uses bundled Natural Earth 110m open data; ASC/DSC rise-set curves, MC/IC meridians, equator/ecliptic/tropics and Parans are all drawn from analytic formulas with multi-point sampling (fixing full-width horizontal lines, polar-hook breaks, and dateline gaps). Adds relocation analysis (four relocated angles + reading), local space, Parans (off / luminaries / all), 4 basemap styles, and instant light/dark theming. The math self-checks against the `swisseph.azalt()` gold standard (`validate_acg.py` → PASS).
- **New auxiliary boards: Horary + Election**: a Western horary judgment rule-base plus an election chart, with a polished right column, save/reopen for event charts, and AI-analysis mounting.
- **Heluo Lishu (数算) completion**: ① the **huagong method** becomes a **per-technique left-panel option** (earth-king Kun/Gen vs four-cardinal-only, mirroring the Canping method switch); ② annual/monthly fortune rows now show the **exact solar-term crossing time** (Lichun / month-establishing terms); ③ the fate chapter gains 5 classically-derivable verdicts — yin-yang number naming (the 24 named conditions), zhong-zong/zhong-ji, prograde/retrograde number + seasonal range, opposing-body warning, and the comprehensive fate-grade score. Core algorithm unchanged; audited line-by-line against the source text.
- **Fixes & polish**: AI model-provider connectivity hardening (Java + launcher), Daliuren "three transmissions" ordering corrected per the classics, occasional blank block in the astrology right column fixed, Feng Shui module UI rewrite, quick-function dock auto-sizing.
- This release includes Java backend changes (Astro Map / AI proxy) and rebundles `astrostudyboot.jar`; the rest is frontend-only.
