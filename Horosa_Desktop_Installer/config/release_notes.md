- 修复 `推运盘 -> 主限法盘` 在部分本地响应格式下左侧双盘空白的问题；现在兼容直接返回对象和 `Result` 包装两种响应结构。
- 修复 `金口诀` 默认“地分自动随时支持续覆盖”导致将神看起来始终固定为月将的问题；现在只在首次起盘时用时支初始化地分，后续改时间不会再把将神时间变量抵消掉。
- 补充金口诀地分自动策略回归测试，避免后续再次把将神锁死。
- 修复桌面发布链里 `runtime` 资产与 `horosa-latest.json` 可能漂移的问题；发布前现在会强制校验 manifest 与待上传资产的 SHA256 一致，避免 fresh install 因 checksum 不匹配被静默延期。
