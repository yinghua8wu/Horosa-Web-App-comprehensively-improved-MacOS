- “检查更新”现在改成两阶段：先检查并展示结果，再由你手动决定是否开始本次更新，不再一检查就直接开跑。
- 更新进度卡片补上了“不确定总大小”下载态；当服务端没返回 `content-length` 时，会明确显示“正在接收数据”，不再误导性卡在 `0%`。
- 更新 helper 和更新后首次启动恢复模式都做了一轮加固：自动重启失败不再被静默吞掉，更新后的第一次启动也会走更强的恢复路径。
- 安装、修复、更新现在统一先进入“安装审查”，列出 app、本机组件和缓存，再由你决定这次要替换哪些内容。
- 离线路径已收紧为“安装完成后直接可用”：如果离线安装结果损坏，启动页会优先引导你重新安装离线包，不再偷偷转为联网修复。
- 启动页、错误态、空状态继续做了一轮 Mac 化收口：主视图优先展示状态与恢复动作，日志和原始错误退到第二层。
- 修复了主窗口首次启动没有默认铺满的问题；现在初次打开和后续重开都会默认最大化铺满屏幕，但不会进入独立全屏空间。
- 修复了节气盘 `XX星盘` 左上角黄道/分宫制仍显示 `undefined` 的漏修路径，现在会稳定显示当前黄道与分宫制。
- 修复了节气盘 `XX星盘` 不跟随上方黄道与分宫制选择重算的问题；切换后会立即按当前设置重新计算，而不是卡在“回归黄道 + 整宫制”。
- 修复了六壬盘里“卜卦人出生时间”选择 `女` 时，左侧 `行年 -> 性别` 仍显示 `男` 的问题；现在左盘显示与行年计算链保持一致。
- 修复了六壬左盘里“卜卦人出生时间”改成 `女` 后，左盘 `行年 -> 性别` 仍停留在旧值的问题；这次真正补上了六壬左盘重绘时的性别同步，不再只修内部显示函数。
- 本次发布版本号为 `v1.1.6`，并同步发布独立 runtime `1.1.6-runtime1`，确保这次六壬左盘修复进入真正的运行时包。
